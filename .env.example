# KiCad MCP Server Configuration

# Comma-separated paths to KiCad project directories
# KICAD_PROJECT_PATHS=/path/to/projects1,/path/to/projects2

# Default detail level for schematic summaries
# Options: brief, standard, detailed
DEFAULT_SUMMARY_DETAIL_LEVEL=standard

# Include nets in summaries by default
INCLUDE_NETS_IN_SUMMARY=true

# Include power analysis in summaries by default
INCLUDE_POWER_IN_SUMMARY=true

# Default test framework for code generation
# Options: pytest, unittest, arduino, zephyr, st_hal, esp_idf
DEFAULT_TEST_FRAMEWORK=pytest

# Default test type
# Options: connectivity, functional, docs, production
DEFAULT_TEST_TYPE=connectivity
